<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TodoWhere" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/dark_green_500</item>
        <item name="colorPrimaryVariant">@color/dark_green_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/sky_200</item>
        <item name="colorSecondaryVariant">@color/sky_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/sky_200</item>

        <!-- Customize your theme here. -->
        <item name="android:timePickerStyle">@style/TimePicker</item>
        <!-- 타이틀바 숨김 -->
        <item name="windowNoTitle">true</item>

    </style>

    <style name="TimePicker" parent="android:Widget.Material.TimePicker">
        <!-- Primary brand color. -->
        <item name="colorPrimary">#81D4FA</item>
        <item name="colorPrimaryVariant">#B39DDB</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color.-->
        <item name="colorSecondary">#FF7043</item>
        <item name="colorSecondaryVariant">#FFCA28</item>
        <item name="colorOnSecondary">#FFE0B2</item>
        <item name="android:timePickerMode">spinner</item>

    </style>

    <style name="Widget.CalendarView.Custom" parent="android:Widget.CalendarView">
        <item name="android:weekDayTextAppearance">@style/TextAppearance.AppCompat.Medium</item>
        <item name="android:dateTextAppearance">@style/TextAppearance.AppCompat.Medium</item>
    </style>

    <!-- 달력에 사용하는 스타일 -->
    <style name="CustomTextAppearance" parent="TextAppearance.AppCompat.Medium">
        <item name="android:textSize">20sp</item>
        <item name="android:textColor">@color/mcv_text_date_light</item>
        <item name="android:textStyle">bold</item>
        <item name="android:includeFontPadding">false</item>
    </style>

    <!-- 여기서 요일, 달력 안 숫자들의 폰트 설정을 수행할 수 있다 -->
    <style name="CalenderViewCustom" parent="Theme.AppCompat">

        <item name="android:textStyle">bold</item>
    </style>

    <!-- 달력 안의 숫자들에 적용되는 스타일 -->
    <style name="CalenderViewDateCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@android:color/black</item>
        <item name="android:weekNumberColor">@android:color/holo_red_light</item>
    </style>

    <!-- 달력 안의 요일들에 적용되는 스타일 -->
    <style name="CalenderViewWeekCustomText" parent="android:TextAppearance.DeviceDefault.Small">
        <item name="android:textColor">@color/black</item>
        <item name="android:weekNumberColor">@android:color/holo_red_light</item>
    </style>

    <!-- 연, 월을 표시하는 헤더에 적용되는 스타일 -->
    <style name="CalendarWidgetHeader">
        <item name="android:textSize">18sp</item>
        <item name="android:textColor">@android:color/darker_gray</item>
    </style>


</resources>